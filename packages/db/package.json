{
  "name": "@repo/db",
  "version": "0.0.0",
  "dependencies": {
    "@prisma/client": "latest",
    "migration": "^0.3.0"
  },
  "devDependencies": {
    "migrate": "^2.1.0",
    "prisma": "^5.22.0"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
